# Activity Feature

Read-only activity timeline for entity history.

## Components

- `ActivitySection` — loads entity activity feed
- `ActivityTimeline` / `ActivityTimelineItem` — vertical timeline with action badges

## Usage

```tsx
<ActivitySection entityType="project" entityId={project.id} />
```
